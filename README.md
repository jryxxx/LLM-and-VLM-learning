# LLM-and-VLM-learning

## MARK
Git
```bash
git add . 
git commit -m 'commit'
git push -u origin main
```
Terminal
```bash
# set
source /etc/network_turbo
# unset
unset http_proxy && unset https_proxy
```